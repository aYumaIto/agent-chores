# ブランチ管理

- GitHub 上のデフォルトブランチは基本的には main を使用する
  - main が存在しない場合はユーザーに確認すること
  - プロジェクト固有のファイルにデフォルトブランチが記されていた場合はそちらを優先すること
- ブランチを切る前に mainブランチで git pull origin main を実行して最新化する
- プッシュはユーザーが明示的に指示した時のみ実行する
- 作業中にマージ済みのローカルブランチを見つけた場合、削除を提案する
